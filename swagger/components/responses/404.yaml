# swagger/components/responses/404.yaml

description: Not found error
content:
  application/json:
    schema:
      type: object
      required:
        - status
        - message
        - data
      properties:
        status:
          type: integer
          example: 404
        message:
          type: string
          example: Not found error
        data:
          type: object
          required:
            - message
          properties:
            message:
              type: string
              example: 'Contact with id=680df4d5205cad88d821b78e not found'